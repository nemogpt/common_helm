apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "common-helm.fullname" . }}-test-connection"
  labels:
    {{- include "common-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "common-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
